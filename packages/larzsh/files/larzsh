#!/bin/sh
# larzsh - LarzOS login shell entry point.
# Runs the Larzscript shell; falls back to /bin/sh if the interpreter is missing.
if command -v larzscript >/dev/null 2>&1; then
    exec larzscript /usr/lib/larzos/larzsh.lz "$@"
fi
exec /bin/sh "$@"
